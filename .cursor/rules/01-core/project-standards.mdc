---
description: 
globs: 
alwaysApply: true
---
# PROJECT STANDARDS - MANDATORY

## LANGUAGE AND COMMUNICATION RULES - MANDATORY

### **English Only Rule - STRICTLY ENFORCED**
```javascript
// ✅ CORRECT - English only
const userAuthentication = async (credentials) => {
  // Validate user credentials
  if (!credentials.email || !credentials.password) {
    throw new Error('Email and password are required');
  }
  return await authenticateUser(credentials);
};

// ❌ WRONG - No Arabic text allowed
const userAuthentication = async (credentials) => {
  // تحقق من بيانات المستخدم
  if (!credentials.email || !credentials.password) {
    throw new Error('البريد الإلكتروني وكلمة المرور مطلوبان');
  }
  return await authenticateUser(credentials);
};
```

### **Code Documentation Standards - MANDATORY**
- All code comments MUST be in English
- All variable names MUST be in English
- All function names MUST be in English
- All error messages MUST be in English
- All log messages MUST be in English
- All commit messages MUST be in English

### **File and Directory Naming - MANDATORY**
```bash
# ✅ CORRECT - English naming
backend/services/auth_service.py
frontend/components/UserProfile.tsx
docs/api/authentication.md

# ❌ WRONG - No Arabic naming
backend/services/خدمة_المصادقة.py
frontend/components/ملف_المستخدم.tsx
docs/api/المصادقة.md
```

## DEVELOPMENT WORKFLOW STANDARDS - MANDATORY

### **Git Workflow Rules - MANDATORY**
```bash
# ✅ CORRECT - English commit messages
git commit -m "Add user authentication endpoint"
git commit -m "Fix database connection issue"
git commit -m "Update agent performance metrics"

# ❌ WRONG - No Arabic commit messages
git commit -m "إضافة نهاية مصادقة المستخدم"
git commit -m "إصلاح مشكلة اتصال قاعدة البيانات"
```

### **Branch Naming Convention - MANDATORY**
```bash
# Feature branches
feature/user-authentication
feature/agent-training-lab
feature/real-time-chat

# Bug fix branches
bugfix/database-connection
bugfix/agent-performance

# Hotfix branches
hotfix/security-patch
hotfix/critical-bug
```

### **Code Review Requirements - MANDATORY**
1. All pull requests MUST be reviewed by at least one other developer
2. All tests MUST pass before merging
3. Code coverage MUST be at least 80%
4. No Arabic text allowed in any form
5. All new features MUST have documentation

## PROJECT STRUCTURE STANDARDS - MANDATORY

### **Directory Structure Rules - MANDATORY**
```
dpro_aI_agent/
├── backend/           # Python FastAPI backend
│   ├── api/          # API endpoints
│   ├── core/         # Core framework
│   ├── models/       # Database models
│   ├── services/     # Business logic
│   └── test/         # ✅ ALL test files organized here
├── frontend/         # React TypeScript frontend
│   ├── src/
│   │   ├── components/  # Reusable components
│   │   ├── pages/      # Page components with API guides
│   │   ├── services/   # API services
│   │   └── types/      # TypeScript types
└── docs/            # Documentation
    ├── api/         # API documentation
    ├── database/    # Database documentation
    └── deployment/ # Deployment guides
```

### **File Naming Conventions - MANDATORY**
```bash
# Backend Python files - snake_case
user_service.py
auth_controller.py
database_models.py

# Frontend TypeScript files - PascalCase for components, camelCase for utilities
UserProfile.tsx
AuthService.ts
apiUtils.ts

# Configuration files - kebab-case
docker-compose.yml
webpack.config.js
tsconfig.json
```

## CODING STANDARDS - MANDATORY

### **Code Quality Rules - MANDATORY**
1. **No Duplicate Code** - Use functions, classes, or modules
2. **Function Length** - Maximum 50 lines per function
3. **Class Length** - Maximum 300 lines per class
4. **File Length** - Maximum 500 lines per file
5. **Meaningful Names** - Use descriptive variable and function names
6. **Type Annotations** - All Python functions must have type hints
7. **Interface Definitions** - All TypeScript components must have interfaces

### **Error Handling Standards - MANDATORY**
```python
# ✅ CORRECT - Comprehensive error handling
async def create_agent(agent_data: AgentCreate, user_id: int) -> Agent:
    try:
        # Validate input
        if not agent_data.name:
            raise ValidationError("Agent name is required")
        
        # Business logic
        agent = await agent_service.create_agent(agent_data, user_id)
        
        # Log success
        logger.info(f"Agent created: {agent.id} for user {user_id}")
        
        return agent
        
    except ValidationError as e:
        logger.warning(f"Validation error: {e}")
        raise HTTPException(status_code=400, detail=str(e))
    except DatabaseError as e:
        logger.error(f"Database error: {e}")
        raise HTTPException(status_code=500, detail="Internal server error")
    except Exception as e:
        logger.error(f"Unexpected error: {e}")
        raise HTTPException(status_code=500, detail="Internal server error")

# ❌ WRONG - Poor error handling
async def create_agent(agent_data, user_id):
    agent = Agent(**agent_data.dict())
    agent.user_id = user_id
    db.add(agent)
    db.commit()
    return agent
```

### **Logging Standards - MANDATORY**
```python
# ✅ CORRECT - Structured logging
logger.info(
    "User action performed",
    user_id=user_id,
    action="create_agent",
    agent_name=agent_data.name,
    timestamp=datetime.utcnow().isoformat()
)

# ❌ WRONG - Poor logging
print("User created agent")
```

## SECURITY STANDARDS - MANDATORY

### **Input Validation - MANDATORY**
```python
# ✅ CORRECT - Validate all inputs
from pydantic import BaseModel, validator

class AgentCreate(BaseModel):
    name: str
    model_provider: str
    temperature: float
    
    @validator('name')
    def validate_name(cls, v):
        if not v or len(v.strip()) < 3:
            raise ValueError('Name must be at least 3 characters')
        return v.strip()
    
    @validator('temperature')
    def validate_temperature(cls, v):
        if v < 0 or v > 2:
            raise ValueError('Temperature must be between 0 and 2')
        return v
```

### **Authentication Requirements - MANDATORY**
- All API endpoints MUST require authentication (except public endpoints)
- JWT tokens MUST expire within 30 minutes
- Refresh tokens MUST expire within 7 days
- All passwords MUST be hashed with bcrypt
- All API calls MUST be logged with user ID

## PERFORMANCE STANDARDS - MANDATORY

### **Response Time Requirements - MANDATORY**
- API endpoints MUST respond within 200ms (95th percentile)
- Database queries MUST complete within 100ms
- Frontend page loads MUST complete within 2 seconds
- Real-time features MUST have <100ms latency

### **Resource Usage Limits - MANDATORY**
- Backend memory usage MUST not exceed 512MB per instance
- Frontend bundle size MUST not exceed 2MB
- Database connections MUST not exceed 100 per instance
- WebSocket connections MUST not exceed 1000 per instance

## RECENT UPDATES - JANUARY 2025

### **Backend Cleanup - COMPLETED JANUARY 2025**
- ✅ **Test Files Organization:** All test files moved to `/backend/test/` directory
- ✅ **Database Scripts Cleanup:** All database scripts organized in `/backend/test/database/`
- ✅ **Code Structure:** Clean separation of concerns maintained
- ✅ **25+ Files Moved:** check_*, create_*, fix_*, init_*, migrate_*, test_* files properly organized

### **API System Status - JANUARY 2025**
- ✅ **Working APIs:** 87.5% (7/8 main APIs functional)
- ✅ **API Endpoints:** 138+ endpoints implemented
- ✅ **API Documentation:** 65% complete (13/20 guides)
- ⚠️ **Minor Fixes Needed:** Agents API & Chat API database issues
- 🔥 **Custom Field Builder:** Revolutionary ACF-style system planned

### **Custom Field Builder - NEW FEATURE JANUARY 2025**
```typescript
// New mandatory component structure for Custom Fields
interface CustomFieldProps {
  field: CustomField;
  value: any;
  onChange: (value: any) => void;
  error?: string;
}

// All 20 field types must follow this interface
const TextField: React.FC<CustomFieldProps> = ({ field, value, onChange, error }) => {
  // Field implementation with validation
  return (
    <div className="custom-field">
      <label>{field.label}</label>
      <input 
        type="text"
        value={value || ''}
        onChange={(e) => onChange(e.target.value)}
        placeholder={field.placeholder}
      />
      {error && <span className="error">{error}</span>}
    </div>
  );
};
```

### **Database Extension - JANUARY 2025**
- **Current Tables:** 37 tables (100% implemented)
- **New Tables:** 4 additional tables for Custom Field Builder
- **Total Planned:** 41 tables (90% implemented, 10% planned)
- **Field Types:** 20 different field types supported
- **Integration:** 8 existing forms will get custom fields

### **Rules System Updates - MANDATORY**
- **Rule Updates Required:** Every code change must update relevant rules
- **Backend Rules:** Updated with cleanup status and API status
- **Frontend Rules:** Updated with Custom Field Builder components
- **API Rules:** Updated with 138+ endpoints and Custom Field Builder
- **Database Rules:** Updated with 4 new Custom Field Builder tables

### **PowerShell Development Commands - ENHANCED**
```powershell
# Enhanced project management commands for Windows
.\run-simple.ps1 help          # Show all available commands
.\run-simple.ps1 update-rules  # Update project rules with latest changes
.\run-simple.ps1 run-backend   # Start Python FastAPI server
.\run-simple.ps1 run-frontend  # Start React development server
.\run-simple.ps1 clean-backend # Clean up test files in backend
.\run-simple.ps1 status        # Show comprehensive project status
.\run-simple.ps1 install-deps  # Install all dependencies
```

## PROJECT CLEANLINESS RULES - MANDATORY

### **Prohibited File Types - STRICTLY FORBIDDEN**
```bash
# ❌ FORBIDDEN - Temporary/Test Files in Root Directory
test_*.html              # No test HTML files
demo_*.html               # No demo HTML files  
*_celebration.html        # No celebration pages
*_test.html              # No temporary test files
success_*.html           # No success pages
*.ps1 (in root)          # No temporary PowerShell scripts
test_*.py (in root)      # No test Python scripts in root

# ✅ ALLOWED - Only Essential Project Files
index.html (frontend)    # Main frontend entry point
README.md (root only)    # Main project documentation
package.json             # Dependency management
requirements.txt         # Python dependencies
```

### **File Creation Rules - MANDATORY**
1. **NO celebration pages or success pages** - Focus on functional code only
2. **NO temporary test files** - Use proper test directories only
3. **NO demo HTML files** - Create proper UI components instead
4. **NO temporary scripts in root** - Move all scripts to appropriate directories
5. **NO unnecessary README files** - One README.md in root is sufficient
6. **ALL test files MUST be in `/backend/test/` directory ONLY**

### **Clean Project Structure - MANDATORY**
```bash
# ✅ CORRECT - Clean root directory
dpro_aI_agent/
├── backend/              # Backend code only
├── frontend/             # Frontend code only
├── docs/                 # Documentation only
├── README.md             # ✅ ONLY ONE README allowed
├── package.json          # Dependencies
└── .gitignore           # Git configuration

# ❌ WRONG - Cluttered root directory
dpro_aI_agent/
├── test_something.html   # ❌ FORBIDDEN
├── demo_page.html        # ❌ FORBIDDEN
├── celebration.html      # ❌ FORBIDDEN
├── test_script.py        # ❌ FORBIDDEN
├── extra_readme.md       # ❌ FORBIDDEN
└── temp_files/           # ❌ FORBIDDEN
```

### **Development Focus Rules - MANDATORY**
1. **Focus on Production Code** - Create only files needed for the application
2. **No Experimental Files** - Use proper development environments for testing
3. **Organized Test Structure** - All tests in designated test directories
4. **Essential Documentation Only** - Document features, not create demo pages
5. **Professional Standards** - Maintain enterprise-level code organization

### **File Cleanup Rules - MANDATORY**
```bash
# Automatic cleanup required for:
- Temporary test files
- Demo HTML pages
- Celebration pages
- Temporary scripts
- Duplicate README files
- Any non-essential files

# Clean project checklist:
✅ All test files in /backend/test/
✅ No temporary files in root
✅ No demo/celebration pages
✅ Single README.md only
✅ Only essential configuration files
```

### **Enforcement Rules - MANDATORY**
1. **Pre-commit Checks** - Scan for prohibited file types
2. **Code Review Requirements** - Reject PRs with unnecessary files
3. **Automated Cleanup** - Scripts to remove temporary files
4. **Developer Education** - Team training on file organization standards
5. **Continuous Monitoring** - Regular audits of project structure

---

**REMEMBER: English only - no exceptions!**  
**REMEMBER: All test files MUST be in `/backend/test/` directory only!**  
**REMEMBER: NO celebration pages, demo files, or temporary scripts!**  
**REMEMBER: Keep project structure clean and professional!**  
**REMEMBER: Focus on production code, not experimental files!**  
**REMEMBER: Rules must be updated with every code change!**

